<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table {
  width: 80%;
  margin: 20px auto;
  border-collapse: collapse;
  font-size: 12px; /* Slightly increase font size for readability */
  border: 1px solid #1f7646; /* Teal-blue border around the table */
  box-shadow: 0 2px 8px rgba(0, 123, 181, 0.2); /* Subtle shadow for a clean look */
}
th, td {
  border: 1px solid #1f7646; /* Teal-blue borders */
  padding: 10px; /* Balanced padding for a cleaner appearance */
  text-align: center;
  line-height: 1.5; /* Better line height for readability */
}
th {
  background-color: #1f7646; /* Teal-blue background for headers */
  color: white; /* White text for contrast */
  font-weight: bold;
  text-transform: uppercase; /* Make header text stand out */
}
td {
  background-color: #f9f9f9; /* Light background for cells */
  text-align: left;
}
tr:nth-child(even) td {
  background-color: #e3f3fb; /* Light teal-blue for alternate rows */
}
tr:hover {
  background-color: #c5e4f3; /* Highlight row on hover for better usability */
  transition: background-color 0.3s; /* Smooth hover effect */
}
button {
  padding: 6px 12px; /* Balanced button size */
  margin: 4px;
  font-size: 12px; /* Consistent font size */
  color: white;
  background-color: #1f7646; /* Teal-blue background */
  border: none; /* Remove button borders */
  border-radius: 4px; /* Rounded corners */
  cursor: pointer;
  transition: background-color 0.3s, transform 0.2s; /* Smooth transitions */
}
button:hover {
  background-color: #005f89; /* Darker teal-blue on hover */
  transform: scale(1.05); /* Subtle zoom effect */
}
button:active {
  background-color: #004364; /* Even darker teal-blue when clicked */
  transform: scale(0.95); /* Slightly shrink on click */
}

    </style>
</head>
<body>
    <!-- Navbar -->
    <%- include('partials/navbar', { page: 'userManagement', security: security }) %>
    <div>
        <h1>User Information</h1>
    <br><br>
    <table style="border: 1px solid black;">
        <thead style="text-align: left;">
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Position</th>
                <th>Location</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
            
        <tbody>
            <% for (let iCount = 0; iCount < users.length; iCount++) { %>
                <tr>
                    <td><%= users[iCount].user_first_name %> <%= users[iCount].user_last_name %></td>
                    <td><%= users[iCount].user_email %></td>
                    <td><%= users[iCount].user_phone %></td>
                    <td><%= users[iCount].user_position %></td>
                    <td><%= users[iCount].zip %></td>
                    <td>
                        <form action="/editUser/<%= users[iCount].user_id %>" method="get">
                            <button type="submit">Edit</button>
                        </form>
                    </td>
                    <td>
                        <form action="/deleteUser/<%= users[iCount].user_id %>" method="post">
                            <button type="submit">Delete</button>
                        </form>
                    </td>
                </tr>
                
            <% } %>
        </tbody>
    </table>
    <br><br>
    <form action="/addUser" method="get">
        <button type="submit" style="margin-left: 10%;">Add User</button>
    </form>
    <br>
    <form action="/" method="get">
        <button type="submit" style="margin-left: 10%;">Return Home</button>
    </form>
    <br>
    <form action="/logout" method="get">
      <button type="submit" style="margin-left: 10%;">Log Out</button>
  </form>
    </div>
    
</body>
</html>
