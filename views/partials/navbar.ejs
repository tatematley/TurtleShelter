<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Turtle Shelter Project</title>
    <!-- Add Bootstrap JS and Popper.js -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
         /* General Styling */
         body {
            font-family: 'Oswald', sans-serif;
            margin: 0;
            padding: 0;
        }
        /* Navbar Styling */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            background-color: black;
        }

        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
            color: #ffffff;
            text-decoration: none;
            
        }

        .navbar-nav {
            display: flex;
            gap: 20px; /* Add spacing between items */
        }

        .navbar-nav a {
            color: #ffffff;
            text-decoration: none;
            padding: 10px 15px;
            transition: background-color 0.3s, color 0.3s; /* Smooth hover effect */
        }

        .navbar-nav a:hover {
            background-color: rgba(255, 255, 255, 0.1);
            color: #0c7466ea;
        }

        .nav-link.active {
            background-color: #0c7466ea(255, 255, 255, 0.1); /* Same as hover background */
            color: #088eb0; /* Same as hover text color */
            font-weight: bold; /* Optional: highlight the current page */
        }

        .navbar-toggler-icon {
    background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='white' stroke-width='2' stroke-linecap='round' stroke-miterlimit='10' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
}


        /* Dropdown button */
        .dropdown .dropbtn {
        font-size: 16px;
        border: none;
        outline: none;
        color: #ffffff;
        padding: 14px 16px;
        background-color: inherit;
        font-family: inherit; /* Important for vertical align on mobile phones */
        margin: 0; /* Important for vertical align on mobile phones */
        }

        /* Dropdown content (hidden by default) */
        .dropdown-content {
        display: none;
        position: absolute;
        background-color: #333; /* Darker background for dropdown */
        min-width: 160px;
        box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
        z-index: 1;
        }

        /* Links inside the dropdown */
        .dropdown-content a {
        float: none;
        color: #fff; /* White text for better visibility */
        padding: 12px 16px;
        text-decoration: none;
        display: block;
        text-align: left;
        }

        /* Add a grey background color to dropdown links on hover */
        .dropdown-content a:hover {
            background-color: #00bfa5; /* Highlight background on hover */
            color: #000; /* Black text for better contrast on hover */
        }

        /* Show the dropdown menu on hover */
        .dropdown:hover .dropdown-content {
        display: block;
        }

        /* Dropdown Menu Background */
        .dropdown-menu {
            background-color: #333 !important; /* Dark background */
            border: none; /* Optional: Remove border */
        }

        /* Dropdown Items */
        .dropdown-item {
            color: #fff !important; /* White text for visibility */
        }

        /* Dropdown Items Hover Effect */
        .dropdown-item:hover {
            background-color: #00bfa5 !important; /* Highlight color */
            color: #000 !important; /* Black text for readability */
        }

    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Turtle Shelter Project</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <!-- links that are always on nav bar -->
                <ul class="navbar-nav ms-auto"> <!-- ms-auto aligns to the right -->
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'home') %>" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'jen') %>" href="/jen">Jen's Story</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'events') %>" href="/events">Events</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'volunteer') %>" href="/volunteer">Volunteer</a>
                    </li>
                    <% if (!security) { %>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'login') %>" href="/login">Login</a>
                    </li>  
                    <% } %>  

                    <!-- if user is logged in, show the manage dropdown in nav bar-->
                    <% if (security) { %>
                        <!-- Dropdown Using Bootstrap -->
                        <div class="dropdown">
                            <button 
                                class="btn btn-secondary dropdown-toggle" 
                                type="button" 
                                id="dropdownMenuButton" 
                                data-bs-toggle="dropdown" 
                                aria-expanded="false"
                                style="background-color: black; border: none; padding-top: 10px;">
                                Manage
                            </button>
                            <!-- links to management pages -->
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton">
                                <li><a class="dropdown-item" href="/eventManagement">Manage Events</a></li>
                                <li><a class="dropdown-item" href="/volunteerManagement">Manage Volunteers</a></li>
                                <li><a class="dropdown-item" href="/userManagement">Manage Users</a></li>
                                <li><a class="dropdown-item" href="/vestDistribution">Vest Distribution</a></li>
                                <li><a class="dropdown-item" href="/dashboard">Dashboard</a></li>
                            </ul>
                        </div>

                    <% } %>        
                </ul>
            </div>
        </div>
    </nav>

    <!-- Add Bootstrap JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>

</body>
</html>
