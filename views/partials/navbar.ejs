<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Turtle Shelter Project</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
         /* General Styling */
         body {
            font-family: 'Palatino', sans-serif;
            margin: 0;
            padding: 0;
        }
        /* Navbar Styling */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            background-color: black;
        }

        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
            color: #ffffff;
            text-decoration: none;
        }

        .navbar-nav {
            display: flex;
            gap: 20px; /* Add spacing between items */
        }

        .navbar-nav a {
            color: #ffffff;
            text-decoration: none;
            padding: 10px 15px;
            transition: background-color 0.3s, color 0.3s; /* Smooth hover effect */
        }

        .navbar-nav a:hover {
            background-color: rgba(255, 255, 255, 0.1);
            color: #00bfa5;
        }

        .nav-link.active {
            background-color: #00bfa5(255, 255, 255, 0.1); /* Same as hover background */
            color: #00bfa5; /* Same as hover text color */
            font-weight: bold; /* Optional: highlight the current page */
        }

        .navbar-toggler {
            border-color: #ffffff;
        }

        .navbar-toggler-icon {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='rgba%280, 0, 0, 0.5%29' stroke-width='2' stroke-linecap='round' stroke-miterlimit='10' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
        }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Turtle Shelter Project</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto"> <!-- ms-auto aligns to the right -->
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'home') %>" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'jen') %>" href="/jen">Jen's Story</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'events') %>" href="/events">Events</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'volunteer') %>" href="/volunteer">Volunteer</a>
                    </li>
                    <% if (!security) { %>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'login') %>" href="/login">Login</a>
                    </li>  
                    <% } %>  
                    <% if (security) { %>
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'eventManagement')  %>" href="/eventManagement">Manage Events</a>
                    </li> 
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'volunteerManagement')  %>" href="/volunteerManagement">Manage Volunteers</a>
                    </li> 
                    <li class="nav-item">
                        <a class="nav-link <%= (page === 'userManagement') %>" href="/userManagement">Manage Users</a>
                    </li> 
                    <% } %>        
                </ul>
            </div>
        </div>
    </nav>

    <!-- Add Bootstrap JS and Popper.js -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>

